export enum CarType {
  SEDAN = "SEDAN"
}

export enum CarTransmission {
  MANUAL = "MANUAL",
  AUTO = "AUTO"
}

export enum CarFuel {
  GREENGAS = "GREENGAS",
  DISEL = "DISEL"
}

export enum CarState {
  NEW = "NEW",
  USED = "USED",
  OLD = "OLD"
}

export enum CarStatus {
  PAUSE = "PAUSE",
  PROCESS = "PROCESS",
  DELETE = "DELETE",
}